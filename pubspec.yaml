name: auralis_reader
description: Auralis Reader — leitor universal multiplataforma com TTS neural local.
publish_to: 'none'
version: 2.0.1+11

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: '>=3.47.0'

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.3.0
  audioplayers: ^6.8.1
  crypto: ^3.0.6
  file_picker: ^13.1.0
  flutter_tts: ^4.2.5
  html: ^0.15.7
  path: ^1.9.1
  path_provider: ^2.1.6
  pdfrx: ^2.6.5
  sherpa_onnx: ^1.13.8
  xml: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
